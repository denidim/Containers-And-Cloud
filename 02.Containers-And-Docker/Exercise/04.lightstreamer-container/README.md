# 04.Lightstreamer Container

> Lightstreamer Container

## Docker commands

``` bash
# runn container on localhost:80 in detached mode with name ls-server
docker run -p 80:8080 --name ls-server -d lightstreamer:latest
```